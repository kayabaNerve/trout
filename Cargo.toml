[workspace]
resolver = "3"
members = [
  "class-groups",
  "bicycl",
  "two-round-ecdsa",
]

[profile.dev.package]
crypto-bigint = { opt-level = 3 }
crypto-primes = { opt-level = 3 }
class-groups = { opt-level = 3 }

[workspace.lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage)'] }
# https://doc.rust-lang.org/rustc/lints/listing/allowed-by-default.html
ambiguous_negative_literals = "warn"
closure_returning_async_block = "warn"
deprecated_safe = "warn"
deref_into_dyn_supertrait = "warn"
explicit_outlives_requirements = "warn"
impl_trait_redundant_captures = "warn"
macro_use_extern_crate = "warn"
meta_variable_misuse = "warn"
missing_docs = "warn"
non_ascii_idents = "warn"
redundant_lifetimes = "warn"
single_use_lifetimes = "warn"
trivial_casts = "warn"
unit_bindings = "warn"
unsafe_attr_outside_unsafe = "warn"
unused_extern_crates = "warn"
unused_import_braces = "warn"
unused_lifetimes = "warn"
unused_macro_rules = "warn"

[workspace.lints.clippy]
# `x % 2` is simple and straightforward
manual_is_multiple_of = "allow"
# `.unwrap_or(x)` unwraps to `x`. This lint then errors if `x == Default::default()`.
# Wanting to unwrap to `x` does not mean the caller wants to unwrap to `Default::default()`.
# While this asserts the current value of `Default::default()`, it doesn't guarantee that value
# will remain the default moving forward, which risks the unwrapped value changing unexpectedly.
unwrap_or_default = "allow"
# `.map(f).unwrap_or(x)` is clearer than `.map_or(x, f)`.
map_unwrap_or = "allow"
# Explicit `continue`s can increase readability.
needless_continue = "allow"
# `clippy` really doesn't like our pseudo-LaTeX
doc_markdown = "allow"

# Enable the off-by-default lint categories, before disabling those we don't want
pedantic = { level = "warn", priority = -1 }
case_sensitive_file_extension_comparisons = "allow"
explicit_deref_methods = "allow"
if_not_else = "allow"
inconsistent_struct_constructor = "allow"
items_after_statements = "allow"
many_single_char_names = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
naive_bytecount = "allow" # We don't want a suggestion to add an external dependency
similar_names = "allow"
# This triggers on the exhaustive `match opt { Some => {}, None => {} }`, which is excessive
single_match_else = "allow"
struct_field_names = "allow"
too_many_lines = "allow"
wildcard_imports = "allow"

style = { level = "warn", priority = -1 }
blocks_in_conditions = "allow"
items_after_test_module = "allow"

# Cherry-pick lints from the `restrictions` group
alloc_instead_of_core = "warn"
as_conversions = "warn"
as_pointer_underscore = "warn"
as_underscore = "warn"
assertions_on_result_states = "warn"
cfg_not_test = "warn"
create_dir = "warn"
dbg_macro = "warn"
filetype_is_file = "warn"
float_arithmetic = "warn"
float_cmp_const = "warn"
fn_to_numeric_cast_any = "warn"
get_unwrap = "warn"
if_then_some_else_none = "warn"
iter_over_hash_type = "warn"
lossy_float_literal = "warn"
partial_pub_fields = "warn"
precedence_bits = "warn"
rc_mutex = "warn"
redundant_test_prefix = "warn"
same_name_method = "warn"
std_instead_of_alloc = "warn"
std_instead_of_core = "warn"
str_to_string = "warn"
string_slice = "warn"
suspicious_xor_used_as_pow = "warn"
todo = "warn"
unnecessary_self_imports = "warn"
unused_trait_names = "warn"
wildcard_enum_match_arm = "warn"

# TODO: unused_crate_dependencies = "warn"
# TODO: `-Zcargo-lints`: [workspace.lints.cargo]
# TODO: `-Zcargo-lints`: missing_lints_inheritance = "warn"
# TODO: `-Zcargo-lints`: non_kebab_case_bins = "warn"
# TODO: `-Zcargo-lints`: non_kebab_case_features = "warn"
# TODO: `-Zcargo-lints`: non_kebab_case_packages = "warn"
# TODO: `-Zcargo-lints`: redundant_homepage = "warn"
# TODO: `-Zcargo-lints`: redundant_readme = "warn"
# TODO: `-Zcargo-lints`: unknown_lints = "warn"
# TODO: `-Zcargo-lints`: unused_dependencies = "warn"
# TODO: `-Zcargo-lints`: unused_workspace_dependencies = "warn"
# TODO: `-Zcargo-lints`: unused_workspace_package_fields = "warn"
# TODO: Lint for implicit features?
